import errorSlice from "./errorSlice";
import otpSlice from "./otpSlice";
import { inputSlice, buttonSlice, mainCardSlice } from "./atomSlices";

export { errorSlice, inputSlice, buttonSlice, mainCardSlice, otpSlice };
