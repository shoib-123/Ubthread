const wrapperStyles = {
  display: "grid",
};

const signUpContainer = {
  position: "relative",
  minHeight: "100vh",
  padding: "5rem",
};

const authCardStyles = {
  display: "grid",
  justifyContent: "center",
};

const backgroundImage = {
  position: "absolute",
  width: "100%",
  height: "100%",
  objectFit: "fill",
  zIndex: -1,
};

export { signUpContainer, authCardStyles, backgroundImage, wrapperStyles };
